# need to escape this alias so it doesn't get expanded until run
# EDITOR is set after sourcing this file
alias zshrc="\$EDITOR ~/.dotfiles/zsh/zshrc.symlink"
alias ohmyzsh="\$EDITOR ~/.dotfiles/oh-my-zsh"
